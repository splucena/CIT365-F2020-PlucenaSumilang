@page
@model RazorMegaDesk.Pages.SurfaceMaterials.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SurfaceMaterial[0].Material)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.SurfaceMaterial) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Material)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.SurfaceMaterialID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.SurfaceMaterialID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.SurfaceMaterialID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
