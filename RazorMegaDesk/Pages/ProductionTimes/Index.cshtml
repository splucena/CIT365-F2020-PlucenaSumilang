@page
@model RazorMegaDesk.Pages.ProductionTimes.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProductionTime[0].Days)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductionTime[0].Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ProductionTime) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Days)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ProductionTimeID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ProductionTimeID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ProductionTimeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
